# Laravel Installer

The Laravel Installer is used to create new Laravel projects.

## Installation

The Laravel Installer can be installed globally via Composer.
```bash
composer global require laravel/installer
```

## Create Laravel Project

Create a new project with the `laravel` command.
```bash
laravel new my-project
```
